[project]
name = "real-token-meter"
version = "0.1.0"
description = "Training throughput and cost over real tokens, refusing padding-inflated numbers."
requires-python = ">=3.10"
license = { text = "Evaluation-Only Licence 1.0 (source-available, not open source); see LICENSE" }
authors = [{ name = "Vo Ba Hoang Minh" }]
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
